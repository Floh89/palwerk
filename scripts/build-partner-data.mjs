import fs from 'node:fs';

const readJson=(path,fallback)=>{try{return JSON.parse(fs.readFileSync(path,'utf8'))}catch{return fallback}};
const raw=readJson('data/upstream/partner-skills.json',{});
const scalesRaw=readJson('data/upstream/partner-scales.json',{});
const clean=value=>String(value??'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();
const num=value=>String(value??'').toUpperCase().replace(/[^0-9A-Z]/g,'');
const norm=value=>clean(value).toLowerCase();
const generatedAt=new Date().toISOString();
const GAME_VERSION='1.0';
const SOURCE='palworld-wiki-partner-data';

function rowsOf(value){if(Array.isArray(value))return value.flatMap(rowsOf);if(!value||typeof value!=='object')return[];if(value.pal_name||value.pal_num||value.skill_name)return[value];return Object.values(value).flatMap(rowsOf)}
function cargoRows(value){if(Array.isArray(value))return value.flatMap(cargoRows);if(!value||typeof value!=='object')return[];if(value.title&&typeof value.title==='object')return[value.title];if(value.partnerSkill||value.effectType||value.effectValue)return[value];return Object.values(value).flatMap(cargoRows)}
function elementFrom(text){const raw=String(text??'').toLowerCase(),idMatch=raw.match(/common_element_name_(fire|water|leaf|electricity|electric|ice|ground|earth|dark|dragon|normal)/i),token=idMatch?.[1]?.toLowerCase();const map={fire:'Feuer',water:'Wasser',grass:'Gras',leaf:'Gras',electric:'Elektro',electricity:'Elektro',ice:'Eis',ground:'Erde',earth:'Erde',dark:'Schatten',dragon:'Drache',normal:'Neutral'};if(token&&map[token])return map[token];for(const[key,value]of Object.entries(map))if(new RegExp(`\\b${key}\\b`,'i').test(raw))return value;return null}
function activationFrom(text,type){if(/while in team|while in party|in your party|when in team/.test(text))return'in_party';if(/while mounted|when mounted|can be ridden|riding/.test(text))return'mounted';if(/when assigned to .*base|while assigned to .*base/.test(text))return'base_assigned';if(/assigned to .*ranch|assigned to .*farm/.test(text))return'ranch';if(/when bred|breeding parent|while breeding/.test(text))return'breeding_parent';if(/when deployed in a raid|raid deployed/.test(text))return'raid_deployed';if(/when fighting together|when summoned|while active|follows up player attacks|attacks targeted enemy/.test(text))return'summoned';if(/when it defeats|when .* defeats|more items when defeated/.test(text))return'pal_defeats_target';if(/when picked up|manual pickup|while gathering eggs|collecting eggs/.test(text))return'manual_pickup';if(/player attacks|player's attacks|changes the player's attack type/.test(text))return'player_attack';if(['mount','glider','active_weapon','active_launcher','active_grenade','double_jump'].includes(type))return'mounted';if(type==='farm_drop')return'ranch';if(type==='base_work_suitability_boost')return'base_assigned';return'unknown'}
function equipmentFor(text,type){const required=/requires .*saddle|with .*saddle|using .*saddle|can be ridden|mount/.test(text)||['mount','active_weapon','active_launcher','active_grenade','glider','double_jump'].includes(type);return{requiresEquipment:required,equipmentId:required?'pal-specific-equipment':null}}
function stackingFor(type,element,target){const group=[type,element||target||'general'].join(':');const knownNonStackable=['mount','glider','active_weapon','active_launcher','active_grenade','heal_player','revive_player','double_jump','movement_speed','player_attack_element_conversion','base_work_suitability_boost','farm_drop','pal_element_attack'];return{stackingGroup:group,stackable:knownNonStackable.includes(type)?false:null}}
function appliesToFor(type){if(['player_attack','player_defense','player_weapon_damage','player_attack_element_conversion','heal_player','revive_player','carry_capacity','item_weight_reduction'].includes(type))return['player'];if(['pal_element_attack','pal_attack','party_pal_attack','cooldown_reduction'].includes(type))return['party_pals'];if(type==='pal_drop_bonus')return['loot'];if(['base_work_suitability_boost','farm_drop'].includes(type))return['base'];if(['mount','glider','movement_speed','double_jump'].includes(type))return['movement'];return['special']}
function makeEffect(type,text,data={}){const activation=data.activation||activationFrom(text,type),equipment=equipmentFor(text,type),target=data.targetElement||data.target||null;return{type,activation,requiresEquipment:data.requiresEquipment??equipment.requiresEquipment,equipmentId:data.equipmentId??equipment.equipmentId,...stackingFor(type,data.element,target),target:data.target||null,element:data.element||data.targetElement||null,valuesByRank:null,appliesTo:appliesToFor(type),conditions:[],source:SOURCE,gameVersion:GAME_VERSION,verifiedAt:null,confidence:'low',status:'provisional',...data}}
function effectsFor(description){const raw=String(description??''),t=norm(raw),effects=[],element=elementFrom(raw);const push=(type,data={})=>{const effect=makeEffect(type,t,{...data,element:data.element??element}),key=JSON.stringify([effect.type,effect.activation,effect.element,effect.target]);if(!effects.some(e=>JSON.stringify([e.type,e.activation,e.element,e.target])===key))effects.push(effect)};if(/drop more items|more items when defeated|increases.*drops/.test(t))push('pal_drop_bonus',{targetElement:element,target:element});if(/increases attack power of .* pals|increases attack of .* pals|enhances .* attacks/.test(t))push('pal_element_attack',{element});if(/max carrying capacity|carry supplies|carrying capacity/.test(t))push('carry_capacity');if(/can be ridden as an? flying mount/.test(t))push('mount',{target:'flying'});else if(/can be ridden/.test(t))push('mount',{target:'ground'});if(/glider|gliding/.test(t))push('glider');if(/restore.*hp|heals? the player|restore the player's hp/.test(t))push('heal_player');if(/revive|resuscitate/.test(t))push('revive_player');if(/life steal|absorbs some.*damage.*restores/.test(t))push('life_steal');if(/player's defense|increases the player's defense/.test(t))push('player_defense');if(/player's attack|increases attack/.test(t)&&!/attack power of .* pals|attack of .* pals/.test(t))push('player_attack');if(/applies .* damage to the player's attacks|changes the player's attack type/.test(t))push('player_attack_element_conversion',{element});if(/double jump/.test(t))push('double_jump');if(/cutting trees|destroying boulders|mining efficiency/.test(t))push(/tree/.test(t)?'logging_efficiency':'mining_efficiency');if(/when assigned to .*farm|assigned to .*ranch|sometimes produces|sometimes drops|digs up/.test(t))push('farm_drop');if(/base.*work suitability|increases .* work suitability level/.test(t))push('base_work_suitability_boost');if(/submachine gun|assault rifle|gatling|launcher|grenade|flamethrower/.test(t))push('active_weapon');if(/follows up player attacks|additional attack|attacks targeted enemy/.test(t))push('party_follow_attack');if(/cooldown/.test(t))push('cooldown_reduction');if(/movement speed|move speed|faster while mounted/.test(t))push('movement_speed');return effects}
function parseScaleValue(value){if(value==null||clean(value)==='')return null;const match=clean(value).replace(',','.').match(/-?\d+(?:\.\d+)?/);return match?Number(match[0]):null}
function scaleRows(value){return cargoRows(value).map(row=>{const rawValue=row.effectValue??row.effect_value??row.value??row.Value??row.scale??row.Scale??null;return{skill:clean(row.partnerSkill||row.skill||row.skill_name||row.Skill||row._pageName),effectType:clean(row.effectType||row.effect_type||row.Effect_Type||row.effect||row.Effect),target:clean(row.target||row.Target),level:Number(row.level??row.Level??row.rank??row.Rank),value:parseScaleValue(rawValue),rawValue:clean(rawValue),unit:clean(rawValue).includes('%')?'percent':null}}).filter(row=>row.skill&&Number.isInteger(row.level)&&row.level>=0&&row.level<=4&&row.value!==null)}
function scaleGroups(rows){const groups=new Map();for(const row of rows){const key=`${norm(row.effectType)}|${norm(row.target)}`;if(!groups.has(key))groups.set(key,{effectType:row.effectType,target:row.target,rows:[]});groups.get(key).rows.push(row)}return[...groups.values()]}
function valuesByRank(rows){if(!rows.length)return null;const values=Array(5).fill(null);for(const row of rows)values[row.level]=row.value;return values.every(value=>value!==null)?values:null}
function targetInfo(target){const t=norm(target),element=elementFrom(target);return{raw:t,element,isTrainer:/trainer|player/.test(t),isSelf:/\bself\b/.test(t),isPals:/pal/.test(t)}}
function semanticFromScale(group){const type=norm(group.effectType),target=targetInfo(group.target),values=valuesByRank(group.rows);if(!values)return null;
  if(/item.*drop|drop.*rate|drop.*amount|loot/.test(type))return{type:'pal_drop_bonus',activation:'pal_defeats_target',element:target.element,target:target.element||group.target};
  if(/weak.*spot|weakpoint/.test(type))return{type:'weakpoint_damage',activation:target.isTrainer?'summoned':'in_party',element:target.element,target:group.target};
  if(/cooldown/.test(type))return{type:'cooldown_reduction',activation:'in_party',element:target.element,target:group.target};
  if(/life.*steal/.test(type))return{type:'life_steal',activation:'summoned',target:group.target};
  if(/damage.*reduction|received.*damage/.test(type))return{type:'damage_reduction',activation:target.isTrainer?'summoned':'in_party',element:target.element,target:group.target};
  if(/defen/.test(type)&&target.isTrainer)return{type:'player_defense',activation:'in_party',target:'player'};
  if(/attack/.test(type)&&target.isTrainer)return{type:'player_attack',activation:'in_party',target:'player'};
  if(/attack/.test(type)&&target.element&&target.isPals)return{type:'pal_element_attack',activation:'in_party',element:target.element,target:target.element};
  if(/elementboost_/.test(type)&&target.isSelf)return{type:'mount',activation:'mounted',element:elementFrom(group.effectType),target:'self'};
  if(/movement.*speed|move.*speed|ride.*speed/.test(type))return{type:'movement_speed',activation:'mounted',target:group.target};
  if(/weight|carry|inventory/.test(type))return{type:'carry_capacity',activation:'in_party',target:'player'};
  if(/heal|hp.*recover|recovery/.test(type)&&target.isTrainer)return{type:'heal_player',activation:'summoned',target:'player'};
  return null;
}
function effectFromScaleGroup(group,description){const semantic=semanticFromScale(group);if(!semantic)return null;const values=valuesByRank(group.rows);if(!values)return null;return makeEffect(semantic.type,norm(description),{...semantic,valuesByRank:values,confidence:'high',status:'community-tested',verifiedAt:generatedAt,scaleEffectType:group.effectType,scaleTarget:group.target,source:`${SOURCE}:cargo`});}
function scaleMatchesEffect(effect,group){const semantic=semanticFromScale(group);if(semantic){if(semantic.type!==effect.type)return false;if(effect.element&&semantic.element&&norm(effect.element)!==norm(semantic.element))return false;return true;}const type=norm(group.effectType),target=norm(group.target);if(effect.type==='pal_drop_bonus')return/drop|item/.test(type);if(effect.type==='pal_element_attack')return/element|boost|attack/.test(type)&&!/(trainer|player)/.test(target);if(effect.type==='carry_capacity')return/weight|carry|inventory/.test(type);if(effect.type==='player_attack')return/attack/.test(type)&&/(trainer|player)/.test(target);if(effect.type==='player_defense')return/defen/.test(type)&&/(trainer|player)/.test(target);if(effect.type==='cooldown_reduction')return/cooldown/.test(type);if(effect.type==='movement_speed')return/speed/.test(type);if(effect.type==='heal_player')return/heal|hp/.test(type);if(effect.type==='party_follow_attack')return/follow|attack/.test(type);if(effect.type==='mount')return/attack|speed|mount|elementboost/.test(type);return false}
function scaleForEffect(effect,groups,effectCount){const matches=groups.filter(group=>scaleMatchesEffect(effect,group));if(matches.length===1)return matches[0];if(groups.length===1&&effectCount===1)return groups[0];return null}
function effectKey(effect){return JSON.stringify([effect.type,effect.activation,effect.element||null,effect.target||null,effect.stackingGroup||null])}

const scaleRowsAll=scaleRows(scalesRaw),scaleIndex=new Map();for(const row of scaleRowsAll){const key=norm(row.skill);if(!scaleIndex.has(key))scaleIndex.set(key,[]);scaleIndex.get(key).push(row)}
const byPaldeck=new Map();for(const row of rowsOf(raw)){const paldeck=num(row.pal_num||row.paldeck||row.pal_number);if(!paldeck||paldeck==='NA')continue;const skillName=clean(row.skill_name||row.skill||'Partner Skill'),rawDescription=String(row.description??''),description=clean(rawDescription),scales=(scaleIndex.get(norm(skillName))||[]).sort((a,b)=>a.level-b.level),groups=scaleGroups(scales),typedEffects=effectsFor(rawDescription),descriptionEffects=typedEffects.map(effect=>{const group=scaleForEffect(effect,groups,typedEffects.length),rankValues=group?valuesByRank(group.rows):null;return rankValues?{...effect,valuesByRank:rankValues,confidence:'high',status:'community-tested',verifiedAt:generatedAt,scaleEffectType:group.effectType,scaleTarget:group.target,source:`${SOURCE}:cargo`}:effect}),cargoEffects=groups.map(group=>effectFromScaleGroup(group,rawDescription)).filter(Boolean),effects=[...new Map([...descriptionEffects,...cargoEffects].map(effect=>[effectKey(effect),effect])).values()],entry={palName:clean(row.pal_name),paldeck,skillName,description,effects,scales,source:SOURCE,gameVersion:GAME_VERSION,generatedAt},existing=byPaldeck.get(paldeck);if(!existing||entry.description.length>existing.description.length)byPaldeck.set(paldeck,entry)}
const partnerData=[...byPaldeck.values()].sort((a,b)=>a.paldeck.localeCompare(b.paldeck,undefined,{numeric:true}));
const allEffects=partnerData.flatMap(row=>row.effects),combatInParty=allEffects.filter(effect=>effect.activation==='in_party'&&['pal_element_attack','pal_attack','party_pal_attack','cooldown_reduction','player_attack','player_defense','damage_reduction'].includes(effect.type));
const report={generatedAt,partnerEntries:partnerData.length,effectCount:allEffects.length,withTypedEffects:partnerData.filter(x=>x.effects.length).length,withRankValues:allEffects.filter(x=>Array.isArray(x.valuesByRank)).length,combatInParty:combatInParty.length,combatInPartyWithRankValues:combatInParty.filter(x=>Array.isArray(x.valuesByRank)).length,scaleRows:scaleRowsAll.length,cargoDerivedEffects:allEffects.filter(x=>String(x.source).includes(':cargo')).length,unknownActivation:allEffects.filter(x=>x.activation==='unknown').length,unknownStackability:allEffects.filter(x=>x.stackable===null).length};
const out=`import './generated-core.js';\nimport { PAL_CATALOG } from './catalog.js';\nexport const PARTNER_EFFECT_SCHEMA_VERSION='2.2.0';\nexport const PARTNER_DATA=${JSON.stringify(partnerData)};\nexport const PARTNER_REPORT=${JSON.stringify(report)};\nconst normNum=v=>String(v??'').toUpperCase().replace(/[^0-9A-Z]/g,'');\nconst byNum=new Map(PARTNER_DATA.map(row=>[normNum(row.paldeck),row]));\nfor(const pal of PAL_CATALOG){const row=byNum.get(normNum(pal.paldeck))||null;const prior=(pal.partner?.effects||[]).filter(e=>e.type!=='pending_partner_text');const normalized=prior.map(e=>({...e,activation:e.activation||'unknown',requiresEquipment:Boolean(e.requiresEquipment),equipmentId:e.equipmentId||null,stackingGroup:e.stackingGroup||[e.type,e.element||e.targetElement||e.target||'general'].join(':'),stackable:e.stackable??null,target:e.target||e.targetElement||null,element:e.element||e.targetElement||null,valuesByRank:Array.isArray(e.valuesByRank)&&e.valuesByRank.length===5?e.valuesByRank:null,appliesTo:Array.isArray(e.appliesTo)?e.appliesTo:['special'],conditions:Array.isArray(e.conditions)?e.conditions:[],source:e.source||'legacy-palwerk-data',gameVersion:e.gameVersion||null,verifiedAt:e.verifiedAt||null,confidence:e.confidence||'low',status:e.status||'provisional'}));const additions=row?.effects||[];const effects=[...new Map([...normalized,...additions].map(e=>[JSON.stringify([e.type,e.activation,e.element,e.target,e.stackingGroup]),e])).values()];const fallback={type:'partner_description_only',activation:'unknown',requiresEquipment:false,equipmentId:null,stackingGroup:'partner_description_only',stackable:false,target:null,element:null,valuesByRank:null,appliesTo:['special'],conditions:[],source:row?.source||'legacy-palwerk-data',gameVersion:row?.gameVersion||null,verifiedAt:null,confidence:'low',status:'missing'};pal.partner={...(pal.partner||{}),name:row?.skillName||pal.partner?.name||'Partnerfähigkeit nicht strukturiert',description:row?.description||pal.partner?.description||'Keine belastbare Beschreibung verfügbar.',effects:effects.length?effects:[fallback],source:row?.source||pal.partner?.source||'legacy-palwerk-data',gameVersion:row?.gameVersion||pal.partner?.gameVersion||null};pal.condensation={status:row?.scales?.length?'community-tested':'missing',levels:row?.scales||[],source:row?.source||'legacy-palwerk-data',gameVersion:row?.gameVersion||null};}\n`;
fs.writeFileSync('src/generated-partner-data.js',out);
fs.writeFileSync('partner-build-report.json',JSON.stringify(report,null,2));
console.log(JSON.stringify(report,null,2));
