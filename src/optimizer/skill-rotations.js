import { ELEMENT_COUNTER, encounterPhases, normalizeElement } from '../knowledge/combat-graph.js';
import { resolveSkill } from '../data/skills.js';

const N=value=>Number(value)||0;
const clamp=(value,min,max)=>Math.min(max,Math.max(min,value));
const palElements=pal=>(pal?.elements||String(pal?.element||'').split('/')).map(normalizeElement).filter(Boolean);

function assumptionsFor(skill){const assumptions=[];if(skill.animation.value==null)assumptions.push(`${skill.name}: Animation 1,25 s`);if(skill.projectileTime.value==null)assumptions.push(`${skill.name}: Projektilzeit 0,25 s`);if(skill.hitRate.value==null)assumptions.push(`${skill.name}: Trefferquote 72 %`);if(skill.multiHit.value==null)assumptions.push(`${skill.name}: ein Treffer`);return assumptions;}
function bossFit(skill,encounter){const phases=encounterPhases(encounter);let score=0,total=0;for(const phase of phases){const weight=N(phase.hpShare)||1,counters=new Set((phase.elements||[]).map(element=>ELEMENT_COUNTER[element]).filter(Boolean));let fit=counters.has(skill.element)?1.5:1;if(phase.resistances?.[skill.element]!=null)fit*=Math.max(.1,1-N(phase.resistances[skill.element]));if(phase.adds?.length&&N(skill.aoe.value)>0)fit*=1.08;score+=weight*fit;total+=weight;}return total?score/total:1;}
export function evaluateSkill(skillId,{pal,encounter={},defaults={}}={}){const skill=typeof skillId==='object'&&skillId.rawPower?skillId:resolveSkill(skillId);if(!skill)return{status:'missing-skill',skillId};const power=N(skill.rawPower.value),cooldown=Math.max(.25,N(skill.cooldown.value)||10),animation=skill.animation.value==null?(defaults.animation??1.25):N(skill.animation.value),projectile=skill.projectileTime.value==null?(defaults.projectileTime??.25):N(skill.projectileTime.value),hitRate=skill.hitRate.value==null?(defaults.hitRate??.72):clamp(N(skill.hitRate.value),0,1),hits=skill.multiHit.value==null?1:Math.max(1,N(skill.multiHit.value)),channel=N(skill.channeling.value),stab=palElements(pal).includes(normalizeElement(skill.element))?1.2:1,fit=bossFit(skill,encounter),activeTime=Math.max(.05,animation+projectile+channel),cycle=Math.max(.25,cooldown+activeTime),effectivePower=power*hits*hitRate*stab*fit;return{status:power>0?'ok':'insufficient-data',skill,rawPower:power,effectivePower,activeTime,cycleTime:cycle,modelDps:power>0?effectivePower/cycle:0,stab,hitRate,hits,bossFit:fit,assumptions:assumptionsFor(skill),dataQuality:[skill.rawPower.status,skill.cooldown.status,skill.animation.status,skill.hitRate.status].includes('missing')?'modelled':'verified'};}

export function simulateRotation(skillIds,{pal,encounter={},duration=60,defaults={}}={}){const skills=skillIds.map(id=>evaluateSkill(id,{pal,encounter,defaults})).filter(row=>row.status==='ok');if(!skills.length)return{status:'insufficient-data',events:[],modelDamage:0,modelDpsRange:null,assumptions:['Keine auswertbaren Skills vorhanden.']};const ready=new Map(skills.map(row=>[row.skill.id,0])),events=[];let time=0,damage=0;while(time<duration){const available=skills.filter(row=>(ready.get(row.skill.id)||0)<=time).sort((a,b)=>b.modelDps-a.modelDps);if(!available.length){time=Math.min(...ready.values());continue;}const chosen=available[0],start=time,end=Math.min(duration,start+chosen.activeTime);if(start>=duration)break;const applied=chosen.effectivePower*Math.min(1,(duration-start)/chosen.activeTime);damage+=applied;events.push({time:start,skillId:chosen.skill.id,name:chosen.skill.name,modelDamage:applied});ready.set(chosen.skill.id,start+Math.max(.25,N(chosen.skill.cooldown.value)||10));time=end;}const assumptions=[...new Set(skills.flatMap(row=>row.assumptions))];const qualityPenalty=assumptions.length?.75:1,modelDps=damage/duration;return{status:'ok',events,modelDamage:damage,modelDps,modelDpsRange:{low:modelDps*.65*qualityPenalty,high:modelDps*1.05},assumptions,dataQuality:assumptions.length?'modelled':'verified'};}

function combinations(items,choose,start=0,prefix=[],out=[]){if(prefix.length===choose){out.push(prefix);return out;}for(let i=start;i<=items.length-(choose-prefix.length);i++)combinations(items,choose,i+1,[...prefix,items[i]],out);return out;}
export function optimizeSkillRotation({pal,encounter={},availableSkillIds=[],duration=60,limit=3}={}){const evaluated=availableSkillIds.map(id=>evaluateSkill(id,{pal,encounter})).filter(row=>row.status==='ok').sort((a,b)=>b.modelDps-a.modelDps);if(evaluated.length<limit)return{status:'insufficient-data',reason:`Mindestens ${limit} auswertbare Skills erforderlich.`,rotations:[]};const pool=evaluated.slice(0,12),sets=combinations(pool,limit).map(rows=>{const simulation=simulateRotation(rows.map(row=>row.skill.id),{pal,encounter,duration});return{skillIds:rows.map(row=>row.skill.id),skills:rows.map(row=>row.skill),simulation,score:simulation.modelDpsRange?.low||0};}).sort((a,b)=>b.score-a.score);return{status:'ok',rotations:sets.slice(0,5),winner:sets[0],whyWinner:sets[1]?`Rotation A erzielt im konservativen Modell ${Math.round((sets[0].score/Math.max(.001,sets[1].score)-1)*100)} % mehr als Rotation B.`:'Einzige belastbare Rotation.',assumptions:['Ereignisbasiertes Cooldown-Modell.','Fehlende Werte werden konservativ modelliert und ausgewiesen.','Keine sekundengenaue reale DPS-Garantie.']};}
